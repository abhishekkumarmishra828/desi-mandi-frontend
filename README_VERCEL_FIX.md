# Desi Mandi Frontend - Vercel Ready

Extract this ZIP and upload/push files directly in GitHub repo root.

Correct structure:
index.html
login.html
dashboard.html
admin-login.html
css/
js/
assets/
images/
vercel.json

Do not keep files inside public folder for Vercel static deployment.

VS Code commands:
git add .
git commit -m "Fix Vercel frontend routes"
git push

Vercel settings:
Framework Preset: Other
Build Command: empty
Output Directory: empty
Root Directory: blank / ./
